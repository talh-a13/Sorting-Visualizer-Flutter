name: algorithms
description: A new Flutter application.

version: 1.0.0+1

environment:
  sdk: 3.1.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  fl_chart: 0.35.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
